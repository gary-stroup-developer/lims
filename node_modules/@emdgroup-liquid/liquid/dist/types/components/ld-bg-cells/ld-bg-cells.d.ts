import '../../components';
export declare type CellType = 'bioreliance' | 'f' | 'hexagon' | 'millipore' | 'qa-x2f-qc' | 'safc' | 'sigma-aldrich' | 't' | 'tile';
/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 * @part pattern - Element containing the cells pattern
 * @part content - Element wrapping the slot
 */
export declare class LdBgCells {
  /** Cells pattern */
  type: CellType;
  render(): any;
}
